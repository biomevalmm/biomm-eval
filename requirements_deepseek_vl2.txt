-r requirements.txt

# DeepSeek-VL2 execution environment
#
# Install the CUDA-specific PyTorch build separately before installing
# this file. Install the DeepSeek-VL2 repository separately with
# `pip install -e . --no-deps`, as documented in README.md.

transformers==4.38.2
huggingface-hub==0.36.2
tokenizers==0.15.2
safetensors==0.7.0

xformers==0.0.35
triton==3.6.0

accelerate>=1.0,<2
einops>=0.8,<1
sentencepiece>=0.2,<1
timm>=1.0,<2
attrdict>=2.0,<3

filelock==3.29.0
fsspec==2025.3.0
jinja2==3.1.6
networkx==3.6.1
regex>=2025.11,<2027
sympy==1.14.0
